services:
  - type: web
    name: fuzebox-telemetry
    runtime: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
      - key: FUZEBOX_API_KEY
        value: ""
    disk:
      name: fuzebox-data
      mountPath: /var/data
      sizeGB: 1
